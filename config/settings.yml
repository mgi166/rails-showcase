defaults: &defaults
  repository:
    orders:
      - stargazers_count
      - forks_count
      - pushed_at
  bulk_importer:
    user_columns:
      - login
      - avatar_url
      - html_url
    repository_columns:
      - name
      - name_with_owner
      - description
      - html_url
      - stargazers_count
      - forks_count
      - pushed_at
  redis:
    url: redis://localhost:6379/1

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
